# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local

# Citation resolution: the caches are committed (they make the check
# reproducible and keep CI off NCBI for citations already resolved), but the
# report and the offline request batches are derived from them.
scripts/citation-report.json
scripts/.ecitmatch/

scripts/.abstract-cache.json
scripts/relevance-report.json
